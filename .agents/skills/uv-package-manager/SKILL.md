# uv package manager

> **Moved:** OpenTide-specific uv workflows are in [`.agents/skills/uv/SKILL.md`](../uv/SKILL.md).

Use `uv sync --group dev`, `uv run …`, and `scripts/ci-local.sh` as documented there.

For advanced upstream patterns (Docker, lockfile strategies, CI caching), see `references/advanced-patterns.md` in this directory.
