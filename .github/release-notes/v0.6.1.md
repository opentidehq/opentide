# 0.6.1 — CI deploy plans, setup path, and promotion flags

23 September 2026. Patch on **0.6.0**.

Upgrade if a CI `STAGING` or `PRODUCTION` plan skipped a changed rule file that sits directly in `objects/rules/`: 0.6.0 left that file out of the plan, and 0.6.1 deploys it. A file in a subdirectory of `objects/rules/` is still not deployed. The missing-checkout failure is still exit `1`, with a different message.

## Install

```bash
pip install opentide==0.6.1
export OPENTIDE_REPO_ROOT=/path/to/detection-repo
opentide validate --strict
```

## Changed

- CI `STAGING` and `PRODUCTION` plans include a changed rule file that sits directly in `objects/rules/`. 0.6.0 matched the git diff against an absolute rules path, so a repo-relative entry such as `objects/rules/rule.yaml` was left out ([#318](https://github.com/OpenTideHQ/opentide/issues/318)).

## Fixed

- `deploy --plan STAGING` or `PRODUCTION` outside a git checkout names the directory it searched and points at `--plan FULL`, instead of `No git repository was found at None`. `FULL` still does not need a checkout ([#310](https://github.com/OpenTideHQ/opentide/issues/310)).
- `opentide setup --path .` from a subdirectory of a git checkout scaffolds that directory. An explicit `--repo` still replaces `.` ([#324](https://github.com/OpenTideHQ/opentide/issues/324)).
- `opentide setup --no-promotion` and `--promotion-target` write `[promotion]` without `--ci` ([#325](https://github.com/OpenTideHQ/opentide/issues/325)).

## Verify

```bash
python -c "import opentide; print(opentide.__version__)"
opentide setup --yes --name "Tutorial Detections" --org "Example Corp" --platform sentinel --path /tmp/tutorial-detections
opentide --repo /tmp/tutorial-detections validate --strict
```

Expect `0.6.1`.

## Links

- [PyPI](https://pypi.org/project/opentide/0.6.1/)
- [GitHub Release](https://github.com/OpenTideHQ/opentide/releases/tag/v0.6.1)
- [CHANGELOG](https://github.com/OpenTideHQ/opentide/blob/development/CHANGELOG.md)
- [Usage → Releases](https://opentide.org/docs/usage/releases/)
- Tracking issue: [#328](https://github.com/OpenTideHQ/opentide/issues/328)

License: [EUPL-1.2](https://eupl.eu/1.2/en/). Issues: [OpenTideHQ/opentide](https://github.com/OpenTideHQ/opentide/issues).
