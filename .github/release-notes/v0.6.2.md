# 0.6.2 — GitLab base commit and GitHub pull-request refs

24 September 2026. Patch on **0.6.1**.

Upgrade if a GitLab `STAGING` or `PRODUCTION` plan crashed with `'Repo' object has no attribute 'lookup_ref'`, or a GitHub `STAGING` plan asked for Azure pull-request variables and then `KeyError`. Both still exit `1`. GitLab reports `No Source Commit Found`. GitHub names `GITHUB_HEAD_REF` and `GITHUB_BASE_REF`.

## Install

```bash
pip install opentide==0.6.2
export OPENTIDE_REPO_ROOT=/path/to/detection-repo
opentide validate --strict
```

## Fixed

- GitLab `STAGING` and `PRODUCTION` with an unset or unknown base SHA exit `1` with `No Source Commit Found` instead of `FATAL: 'Repo' object has no attribute 'lookup_ref'`. The same command still succeeds when both SHAs are in the checkout ([#333](https://github.com/OpenTideHQ/opentide/issues/333)).
- GitHub Actions `STAGING` without `GITHUB_HEAD_REF` and `GITHUB_BASE_REF` exits `1` with `Expected to find GITHUB_HEAD_REF and GITHUB_BASE_REF | Ensure this is running in a Pull Request pipeline`. 0.6.1 asked for Azure `SYSTEM_PULLREQUEST_SOURCEBRANCH` and `SYSTEM_PULLREQUEST_TARGETBRANCHNAME`, then `KeyError while compiling the deployment plan` ([#334](https://github.com/OpenTideHQ/opentide/issues/334)).

## Verify

```bash
python -c "import opentide; print(opentide.__version__)"
opentide setup --yes --name "Tutorial Detections" --org "Example Corp" --platform sentinel --path /tmp/tutorial-detections
opentide --repo /tmp/tutorial-detections validate --strict
```

Expect `0.6.2`.

## Links

- [PyPI](https://pypi.org/project/opentide/0.6.2/)
- [GitHub Release](https://github.com/OpenTideHQ/opentide/releases/tag/v0.6.2)
- [CHANGELOG](https://github.com/OpenTideHQ/opentide/blob/development/CHANGELOG.md)
- [Usage → Releases](https://opentide.org/docs/usage/releases/)
- Tracking issue: [#337](https://github.com/OpenTideHQ/opentide/issues/337)

License: [EUPL-1.2](https://eupl.eu/1.2/en/). Issues: [OpenTideHQ/opentide](https://github.com/OpenTideHQ/opentide/issues).
